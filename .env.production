# Leave this empty in the repository so each deployment uses its own origin.
# Set an exact production URL in the Cloudflare Pages environment once the
# final pages.dev or custom domain has been confirmed.
VITE_SITE_URL=
